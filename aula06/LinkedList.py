from No import Node

class LinkedList(Node):
    def __init__(self):
        self.first = None    # precisa de uma referencia para o primeiro odo da lista
        self.last = Node        # precisa ref para o ultimo nodo da lista  
        self.len_list = 0       # variavel que armazena o tamano da lista

    #metodo para inserir nodo na lista
 
         

